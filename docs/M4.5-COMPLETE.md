# M4.5 — Structured Data / JSON-LD

Status: COMPLETE

## Implemented

- Reusable JSON-LD rendering helpers with safe `<` escaping.
- Geographic province pages expose `BreadcrumbList` and `ItemList`.
- Geographic regency pages expose `BreadcrumbList` and `ItemList`.
- Campground detail pages expose `BreadcrumbList` and `Place`.
- Detail `Place` uses public name, canonical URL, description, image, postal address, and valid coordinates when available.
- No fabricated ratings, reviews, prices, or unsupported schema claims.
- Structured-data URLs are based on `NEXT_PUBLIC_APP_URL`.

## QA

- Production build: PASS.
- Province JSON-LD source: PASS.
- Regency JSON-LD source: PASS.
- Campground detail JSON-LD source: PASS.
- Browser source confirms `application/ld+json` output.

M4.5 is frozen after successful build and runtime QA.
