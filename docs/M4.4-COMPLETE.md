# M4.4 — Internal Linking, Sitemap & SEO Indexing Rules

Status: COMPLETE

## Acceptance

- Production build passes.
- Campground detail breadcrumb links Camping → Province → Regency → Campground.
- Province and regency links use canonical geographic routes.
- Dynamic `/sitemap.xml` includes public campground detail pages and only geographic pages represented by public campground data.
- Empty geographic pages remain accessible but use `noindex,follow` through their route metadata.
- Development indexing remains blocked.
- `robots.txt` disallows localhost and Netlify preview deployments.
- Production-domain robots allow crawling and advertise the production sitemap.
- Canonical metadata uses `NEXT_PUBLIC_APP_URL` through the application metadata base.
- No database migration, RLS relaxation, or browser privilege change was required.

## QA evidence

Runtime QA passed for:
- campground detail geographic breadcrumb;
- `/sitemap.xml`;
- `/robots.txt`.

M4.4 is frozen after successful build and runtime QA.
