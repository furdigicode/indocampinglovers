# M5.2 — Public Submission Security/API COMPLETE

Status: COMPLETE / FROZEN

## Implemented
- Submission tables remain unavailable for direct anon/authenticated browser access.
- Human-friendly submission reference codes.
- Server-only Supabase elevated client protected by `server-only`.
- Server secret uses non-public environment variable.
- Controlled `POST /api/submissions/campgrounds` endpoint.
- Payload size and server-side field validation.
- Province/regency relationship validation.
- Consent validation.
- Submission idempotency key with unique database index.
- Safe public error responses.
- New submissions enter moderation as `pending`.

## Database
- `0009_m5_2_submission_security.sql` applied.
- `0010_m5_2_submission_idempotency.sql` applied.
- M5.2 security verifier: 6/6 PASS.

## QA
- Production build: PASS.
- Valid submission: PASS, HTTP 201 with ICL reference code.
- Duplicate request/idempotency: PASS; same reference and one database row.
- Consent=false: PASS, rejected HTTP 400.
- Province/regency mismatch: PASS, rejected HTTP 400.
- QA submission cleanup: PASS.

## Security boundary
Public contributors cannot directly mutate canonical campground data or read/write moderation queues. Canonical mutation remains reserved for a later explicit moderation workflow.

## Deferred before public launch
Rate limiting / anti-spam enforcement remains required before public launch and will be added as part of M5 hardening. Photo upload receives its own controlled path in M5.4.

M5.2 is frozen after database, build, positive, idempotency, negative, and cleanup QA passed.
